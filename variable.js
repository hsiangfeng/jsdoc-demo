/**
 * 字串
 * @type { string }
 */
const myName = 'Ray';

/**
 * 陣列字串
 * @type { string[] }
 */
const arrStr = ['Ray', 'John'];

/**
 * 陣列數字
 * @type { (string | number)[] }
 */
const arrNum = ['Ray', 'John', 1];

/**
 * 物件
 * @type { { name: string, age: number } }
 */
const obj = {
  name: 'Ray',
  age: 18,
};

/**
 * 物件陣列
 * @type { { name: string, age: number[] } }
 */
const objArr = {
  name: 'Ray',
  age: [ 10, 20, 30 ],
}

/**
 * 陣列物件
 * @type { { name: string, age: number }[] }
 */
const arrObj = [
  {
    name: 'Ray',
    age: 18,
  },
]
